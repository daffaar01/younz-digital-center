'use client';
import {FormEvent,useState} from 'react';import SiteHeader from '../../site-header';
export default function LoginPage(){const[pending,setPending]=useState(false);const[message,setMessage]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setMessage('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const response=await fetch('/backend/v1/auth/customer-login',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({...data,device_name:`Next.js ${navigator.userAgent.slice(0,50)}`})});const payload=await response.json();if(!response.ok){setMessage(String(Object.values(payload.errors||{}).flat()[0]||payload.message));return}localStorage.setItem('ydc_api_token',payload.data.token);window.location.assign('/akun')}catch{setMessage('Koneksi ke Laravel terputus.')}finally{setPending(false)}}return <main><SiteHeader/><section className="auth-page"><form className="auth-card" onSubmit={submit}><p className="eyebrow">Akun pelanggan</p><h1>Masuk ke akun Anda</h1><p>Pantau pesanan yang terhubung dengan profil pelanggan.</p>{message&&<div className="form-error">{message}</div>}<label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button className="button button-dark full-button" disabled={pending}>{pending?'Memeriksa…':'Masuk'}</button><a href="/akun/daftar">Belum punya akun? Daftar</a><a href="/akun/lupa-password">Lupa password</a></form></section></main>}