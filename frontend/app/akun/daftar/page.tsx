'use client';
import {FormEvent,useState} from 'react';import SiteHeader from '../../site-header';
export default function RegisterPage(){const[pending,setPending]=useState(false);const[message,setMessage]=useState('');const[success,setSuccess]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setMessage('');const data=Object.fromEntries(new FormData(e.currentTarget));try{const response=await fetch('/backend/v1/auth/register',{method:'POST',headers:{Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify({...data,device_name:'Next.js web',terms:'1'})});const payload=await response.json();if(!response.ok){setMessage(String(Object.values(payload.errors||{}).flat()[0]||payload.message));return}setSuccess(true);setMessage(payload.message)}catch{setMessage('Koneksi ke Laravel terputus.')}finally{setPending(false)}}return <main><SiteHeader/><section className="auth-page"><form className="auth-card" onSubmit={submit}><p className="eyebrow">Pendaftaran</p><h1>Buat akun pelanggan</h1><p>Pesanan baru akan otomatis terhubung ke akun Anda.</p>{message&&<div className={success?'form-success':'form-error'}>{message}</div>}{!success&&<><label>Nama<input name="name" required minLength={2}/></label><label>Email<input name="email" type="email" required/></label><label>Nomor WhatsApp<input name="phone" required inputMode="tel"/></label><label>Password<input name="password" type="password" required minLength={8}/></label><label>Konfirmasi password<input name="password_confirmation" type="password" required minLength={8}/></label><label className="auth-terms"><input type="checkbox" required/><span>Saya menyetujui penggunaan data untuk pengelolaan pesanan.</span></label><button className="button button-dark full-button" disabled={pending}>{pending?'Membuat akun…':'Daftar'}</button></>}<a href="/akun/masuk">Sudah punya akun? Masuk</a></form></section></main>}