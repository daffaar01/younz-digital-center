'use client';
import {FormEvent,useEffect,useState} from 'react';import AdminHeader from '../admin-header';type Supplier={id:number;name:string;contact_name:string|null;phone:string|null;email:string|null;address:string|null;is_active:boolean;products_count:number};type Data={suppliers:Supplier[];pagination:{current_page:number;last_page:number;total:number}};
export default function Suppliers(){const[data,setData]=useState<Data|null>(null);const[user,setUser]=useState<{name:string;role:{label:string}}|null>(null);const[q,setQ]=useState('');const[page,setPage]=useState(1);const[message,setMessage]=useState('');async function api(path:string,init:RequestInit={}){const token=localStorage.getItem('ydc_staff_token');if(!token){location.replace('/admin/masuk');throw Error('Sesi berakhir.')}const r=await fetch('/backend/v1/staff'+path,{...init,headers:{Accept:'application/json',Authorization:'Bearer '+token,...init.headers}});const p=await r.json();if(!r.ok)throw Error(String(Object.values(p.errors||{}).flat()[0]||p.message));return p}async function load(next=page){const params=new URLSearchParams({page:String(next)});if(q)params.set('q',q);setData((await api('/suppliers?'+params)).data);setPage(next)}useEffect(()=>{const s=localStorage.getItem('ydc_staff_user');if(s)setUser(JSON.parse(s));load(1).catch(e=>setMessage(e.message))},[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();try{const body=Object.fromEntries(new FormData(e.currentTarget));body.is_active='1';const p=await api('/suppliers',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});setMessage(p.message);e.currentTarget.reset();await load(1)}catch(x){setMessage(x instanceof Error?x.message:'Gagal menyimpan.')}}return <main className="admin-page"><AdminHeader name={user?.name||'Pegawai'} role={user?.role.label||'Staff'}/><section className="admin-shell"><header className="admin-welcome"><div><p className="eyebrow">Pembelian</p><h1>Supplier</h1><p>Kelola mitra pemasok produk dan inventory.</p></div></header>{message&&<div className={message.includes('berhasil')?'form-success':'form-error'}>{message}</div>}<form className="directory-create" onSubmit={submit}><label>Nama supplier<input name="name" required/></label><label>Kontak<input name="contact_name"/></label><label>Telepon<input name="phone"/></label><label>Email<input name="email" type="email"/></label><label className="wide">Alamat<textarea name="address" rows={2}/></label><button className="button button-dark">Tambah supplier</button></form><form className="product-search" onSubmit={e=>{e.preventDefault();load(1).catch(x=>setMessage(x.message))}}><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Cari supplier"/><button className="button button-dark">Cari</button></form><section className="directory-list">{!data?<div className="admin-empty">Memuat supplier…</div>:data.suppliers.length===0?<div className="admin-empty">Belum ada supplier.</div>:data.suppliers.map(v=><article key={v.id}><span><strong>{v.name}</strong><small>{v.contact_name||'-'} · {v.phone||'-'}</small><small>{v.email||'-'}</small></span><b>{v.products_count} produk</b><em>{v.is_active?'Aktif':'Nonaktif'}</em></article>)}</section>{data&&data.pagination.last_page>1&&<nav className="admin-pagination"><button disabled={page<=1} onClick={()=>load(page-1)}>← Sebelumnya</button><span>Halaman {page} dari {data.pagination.last_page}</span><button disabled={page>=data.pagination.last_page} onClick={()=>load(page+1)}>Berikutnya →</button></nav>}</section></main>}