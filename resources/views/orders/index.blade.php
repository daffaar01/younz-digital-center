@extends('layouts.app')
@section('content')
<div><p class="text-sm font-bold text-brand-700">ANTREAN LAYANAN</p><h1 class="mt-1 text-3xl font-black text-ink-900">Pesanan jasa</h1></div>
<details class="card mt-6"><summary class="cursor-pointer font-black">+ Buat pesanan di toko</summary><form method="post" action="{{ route('orders.store') }}" enctype="multipart/form-data" class="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@csrf<div class="grid gap-1"><label>Nama pelanggan</label><input name="customer_name" required></div><div class="grid gap-1"><label>WhatsApp</label><input name="customer_phone" required></div><div class="grid gap-1"><label>Jenis</label><select name="type" required>@foreach(['print','fotokopi','scan','ketik','desain','website','aplikasi'] as $type)<option value="{{ $type }}">{{ ucfirst($type) }}</option>@endforeach</select></div><div class="grid gap-1"><label>Layanan</label><select name="service_id"><option value="">Belum ditentukan</option>@foreach($services as $service)<option value="{{ $service->id }}">{{ $service->name }}</option>@endforeach</select></div><div class="grid gap-1 lg:col-span-2"><label>Catatan</label><textarea name="notes" rows="3"></textarea></div><div class="grid gap-1"><label>Deadline</label><input name="deadline_at" type="datetime-local"></div><div class="grid gap-1"><label>File</label><input name="file" type="file"></div><div><button class="btn-primary">Buat pesanan</button></div></form></details>
<div class="mt-6 grid gap-4 lg:grid-cols-2">@forelse($orders as $order)<a href="{{ route('orders.show',$order) }}" class="card block transition hover:border-brand-500"><div class="flex items-start justify-between gap-3"><div><p class="text-xs font-bold text-slate-400">{{ $order->order_number }}</p><h2 class="mt-1 text-lg font-black text-ink-900">{{ $order->customer_name }} · {{ ucfirst($order->type) }}</h2><p class="mt-1 text-sm text-slate-500">{{ $order->customer_phone }} · {{ $order->service?->name ?? 'Layanan belum dipilih' }}</p></div><span class="badge {{ $order->deadline_at?->isPast() && !in_array($order->status->value,['selesai','dibatalkan']) ? 'bg-red-100 text-red-700' : 'bg-emerald-100 text-emerald-700' }}">{{ $order->status->label() }}</span></div><div class="mt-4 flex justify-between border-t border-slate-100 pt-3 text-xs text-slate-500"><span>@can('viewFiles', $order){{ $order->files_count }} file @else File privat @endcan</span><span>{{ $order->deadline_at?->format('d M H:i') ?? 'Tanpa deadline' }}</span></div></a>@empty<p class="text-slate-500">Belum ada pesanan.</p>@endforelse</div><div class="mt-5">{{ $orders->links() }}</div>
@endsection
