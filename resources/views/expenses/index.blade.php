@extends('layouts.app')
@section('content')
<div><p class="text-sm font-bold text-brand-700">KEUANGAN</p><h1 class="mt-1 text-3xl font-black text-ink-900">Pengeluaran</h1></div>
<form method="post" action="{{ route('expenses.store') }}" class="card mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">@csrf<div class="grid gap-1"><label>Kategori</label><select name="expense_category_id">@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></div><div class="grid gap-1"><label>Jumlah</label><input name="amount" type="number" min="1" required></div><div class="grid gap-1"><label>Tanggal</label><input name="expense_date" type="date" value="{{ today()->toDateString() }}" required></div><div class="grid gap-1"><label>Pembayaran</label><select name="payment_method"><option value="cash">Tunai</option><option value="transfer">Transfer</option><option value="qris">QRIS</option><option value="ewallet">E-wallet</option></select></div><div class="grid gap-1 lg:col-span-4"><label>Keterangan</label><input name="description" required></div><div class="flex items-end"><button class="btn-primary">Catat pengeluaran</button></div></form>
<div class="table-wrap mt-6"><table class="data-table"><thead><tr><th>Nomor</th><th>Tanggal</th><th>Kategori</th><th>Keterangan</th><th>Jumlah</th></tr></thead><tbody>@forelse($expenses as $expense)<tr><td class="font-bold">{{ $expense->expense_number }}</td><td>{{ $expense->expense_date->format('d M Y') }}</td><td>{{ $expense->category->name }}</td><td>{{ $expense->description }}</td><td class="font-bold text-red-600">Rp {{ number_format($expense->amount,0,',','.') }}</td></tr>@empty<tr><td colspan="5" class="text-center text-slate-500">Belum ada pengeluaran.</td></tr>@endforelse</tbody></table></div><div class="mt-4">{{ $expenses->links() }}</div>
@endsection
